// use crate::{render::Vertex, math::Point};

// pub fn get_outline(vertices:Vec<Point>){
//   //Find the leftmost point 
//   let mut leftmost_index = 0;
//   for index in vertices.len() {
//     if vertices[index][1] < vertices[index][leftmost_index]{
//       leftmost_index = index
//     }
//   }

// }

// // fn orientation()